# Agent-Native 工作空间

## 这是什么

这是一个调研与思考工作空间，探索 **Agent-Native** 软件设计范式——当软件的主要用户从人类变成 Agent，软件的设计、构建和分发方式会发生什么根本性变化。

不是一个代码项目，是一个思想实验场。

## 核心命题

1. **GUI 是给人类的翻译层，Agent 不需要它。** CLI+Skill 是 Agent 时代的原生交互方式。
2. **产品形态从 App 变成 Skill。** 没有 UI，但它是产品；没有下载，但它有用户——只不过用户是 Agent。
3. **软件从资产变成耗材。** 生产成本趋近于零，按需生成，用完即弃。
4. **中间层消亡。** 软件是人机之间的中间层，管理层是意图与执行之间的中间层，AI 正在同时冲击所有中间层。

## 思想来源

这个工作空间源于用户对两篇文章的阅读与思考：

- 《AI，正在吞噬所有软件》— 从 Andreessen 的 "Software is eating the world" 到 "Software was eaten by AI"，分析了软件资产化终结、Agent 填平人机交互鸿沟、Skill 作为新产品形态、中间层消亡等趋势。作者视角：10 年用户体验设计师 + 30 人公司创始人。
- 《GUI 将死，CLI 才是一切》— CLI+Skill 作为 Agent 时代标准交互协议的判断，CLI-Anything 等项目的实践印证。

用户从这些文章中提炼出自己的判断：下一代软件大部分会转向 CLI+Skill 形式，输入和操作面向 Agent 而非人类，GUI 的角色收缩到结果/过程可视化（且这一步也可由 Agent 完成）。

## 目录结构

- `docs/` — 方法论、设计原则
- `research/` — 调研笔记、案例分析、生态图谱
- `examples/` — 实践验证

## 工作方式

以调研和写作为主，不急于写代码。需要验证想法时再做原型。
